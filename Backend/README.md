# Backend

## Setup

1. Start PostgreSQL with Docker:

```bash
cd Backend
docker compose up -d
```

2. Install dependencies:

```bash
npm install
```

3. Start the server:

```bash
npm run dev
```

```

```
