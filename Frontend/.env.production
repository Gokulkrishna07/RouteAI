# Applied by `npm run build` (mode=production), overriding .env.
# The hosting platform's own environment variables take precedence over this file.
VITE_API_BASE_URL=https://routeai-backend-btbb.onrender.com/api/v1
