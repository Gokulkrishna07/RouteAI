# Applied when vitest runs (mode=test), overriding any local .env.
# Tests mock every HTTP call, so this URL is never dialled — it exists so the
# suite is deterministic and does not depend on a developer's local .env.
VITE_API_BASE_URL=http://localhost:3000/api/v1
